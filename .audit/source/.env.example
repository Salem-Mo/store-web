# Public Supabase settings (safe to expose to the browser when RLS is configured)
NEXT_PUBLIC_SUPABASE_URL=https://epgpdsaslacbpxqptmmk.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_publishable_or_anon_key

# Server-only secret. NEVER commit this value or expose it to the browser.
# Required only by server-side admin operations such as /api/worker-auth.
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
